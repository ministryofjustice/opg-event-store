# opg-event-store

Documentation source for public events between OPG services.

## Running locally

Run `docker compose up server  ` to run locally on port 8089. Any changes to content files will automatically be updated in the hosted site.

Run `docker compose up builder` to run the sass and site build but not serve.

Managed by opg-org-infra &amp; Terraform
